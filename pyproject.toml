[project]
name = "cthulhu-rpg-manager"
version = "0.1.0"
description = "Call of Cthulhu RPG management system"
requires-python = ">=3.13"
dependencies = [
	"Django>=5.3",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

